#include "../inc/philo.h"

void	*phil_routine(void *arg)
{
	t_philo	*phil;

	phil = (t_philo *)arg;
	if (phil->id % 2 == 0)
	{
	}
	else
	{
	}
	return (NULL);
}

int	create_phil(t_info *info)
{
	return (0);
}
